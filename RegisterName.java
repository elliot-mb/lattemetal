
public enum RegisterName {
    zero, //constant zero
    ra, //return address
    sp, //stack pointer
    gp, //global pointer
    tp, //thread pointer
    t0, t1, t2, t3, t4, t5, t6, // temporary registers
    s0, s1, s2, s3, s4, s5, s6, s7, s8, s9, s10, s11, //saved registers
    a0, a1, a2, a3, a4, a5, a6, a7 //argument registers
}
